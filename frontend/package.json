{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf development .cache",
    "dev": "npm run clean && parcel index.html --out-dir development -p 3000",
    "build": "parcel build index.html --out-dir dist --public-url ./"
  },
  "author": "Mateusz Hawrus",
  "license": "ISC",
  "dependencies": {
    "navigo": "^7.1.3",
    "parcel": "^1.12.4"
  }
}
